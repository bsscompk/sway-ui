{
  "name": "sway-ui",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "preview"
  ],
  "scripts": {
    "lib:build": "pnpm -C packages/sway-ui build",
    "preview:dev": "pnpm -C preview dev"
  }
}
